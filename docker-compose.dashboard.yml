services:
  dashboard:
    build:
      context: ./dashboard
      args:
        NEXT_PUBLIC_API_URL: https://agent.dokman.io
    restart: unless-stopped
    ports:
      - "127.0.0.1:9100:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
